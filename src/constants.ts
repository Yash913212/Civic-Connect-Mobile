// ── Greater Hyderabad Bounding Box ──
export const HYDERABAD_BOUNDS: { minLat: number; minLng: number; maxLat: number; maxLng: number } = {
  minLat: 17.25,
  minLng: 78.25,
  maxLat: 17.60,
  maxLng: 78.65,
};

export const HYDERABAD_CENTER: { latitude: number; longitude: number } = {
  latitude: 17.4483,
  longitude: 78.3741,
};

export const DEFAULT_ZOOM = 12;
export const MIN_ZOOM = 10;
export const MAX_ZOOM = 18;

// ── App Info ──
export const APP_NAME = 'Civic Connect';
export const APP_VERSION = '1.0.0';

// ── Limits ──
export const MAX_COMPLAINT_DESC_LENGTH = 500;
export const MAX_PHOTO_SIZE_MB = 10;
